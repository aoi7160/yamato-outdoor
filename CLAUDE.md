# YAMATO OUTDOOR

「山と生きる。山と挑む。」— 実体験ベースの装備検証・レビューを届けるアウトドアメディア。
トーンは硬派・ストイック。軽薄な煽り・曖昧な逃げの表現は避ける。

## 今の事業フォーカス(記事企画・執筆で必ず踏まえること)
1. **登山** — 最優先。運営メンバーに実体験があり、ファクトチェックが早い
2. **釣り・キャンプ** — 今後1年でメンバーと実際に行い、コンテンツ化していく次点の領域
3. その他(天体観測・ブッシュクラフト・無人島サバイバル・スノボ・サイクリング)は将来領域、今は着手しない。ハイキングは登山の一部として自然に扱ってよい

## 技術構成
- Webサイト: Astro(静的生成、`src/`)
- CMS: microCMS(記事データ、詳細は `docs/microcms-schema.md`)
- ホスティング: Cloudflare Pages
- アクセス解析: Umami Cloud(`docs/integrations/umami.md`)

## ドキュメント
- `docs/architecture.md` — 全体構成と、将来何が必要になりそうかのメモ(症状ベース、特定ツール確定ではない)
- `docs/decisions/` — なぜそのOSS/ツールを採用・不採用にしたかの記録(ADR)。新しい判断はここに追記する
- `docs/integrations/` — 実際に採用したサービスの連携仕様のみ。サービス本体のコードは置かない
- `docs/content-workflow.md` — 記事の企画〜公開フロー、平日/週末の役割分担

## AIエージェント(`.claude/agents/`)
記事制作は `seo-researcher` → `outdoor-researcher` → `writer` → `editor` の順に通す。
SNS文言は `social-media-manager`、アクセスデータの分析は `marketing-analyst` を使う。
各エージェントの定義ファイルに、上記の事業フォーカス(登山優先)を必ず反映すること。

## 原則
- 外部OSSの実体コードはこのリポジトリにcloneしない。連携が必要な時は `docs/integrations/` に仕様だけを書く
- 実体験・個人的な感想をAIが捏造しない。書けない箇所は明示的なプレースホルダーとして残し、人間に渡す
- 新しいツールを追加する判断をしたら、必ず `docs/decisions/` にADRを追記する
